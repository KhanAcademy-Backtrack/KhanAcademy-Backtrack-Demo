'use client';
import {khanEntry,KHAN_ENTRIES} from '@/lib/khan-entry';
import {recordKhanOpen,packFromKhan,planSession,beginSession,type Pack} from '@/lib/study';
import {useStudy} from './StudyProvider';
export function PackKhanResources({pack}:{pack:Pack}){const {update}=useStudy();const origin=khanEntry(pack.khanId);const entries=origin?[origin]:pack.topics.map(topic=>KHAN_ENTRIES.find(e=>e.topic===topic&&e.skill==='goal')!).filter(Boolean);
 return <div className="pack-khan-resources"><img src="/khan-academy.svg" alt="Khan Academy" width="122" height="20"/>{entries.map(entry=><div key={entry.id}><span>{entry.title}</span><a href={entry.learning.url} target="_blank" rel="noopener noreferrer" onClick={()=>update(s=>({...s,events:[...s.events,{kind:'khan_open' as const,at:Date.now(),detail:`learning:${entry.topic}:${entry.skill}:${entry.learning.title}`}].slice(-1000),updatedAt:Date.now()}))}>Learn ↗</a><a href={entry.practice.url} target="_blank" rel="noopener noreferrer" onClick={()=>update(s=>{const now=Date.now(),goal=packFromKhan(entry);let prepared={...s,packs:[...s.packs.filter(p=>p.id!==goal.id),goal]};if(s.activeSession?.complete||s.activeSession?.packId!==goal.id)prepared=beginSession(prepared,planSession(prepared,goal,5,'review',now),now);return recordKhanOpen(prepared,{topic:entry.topic,skill:entry.skill,title:entry.practice.title,url:entry.practice.url,returnPath:'/study/session',at:now});})}>Practise ↗</a></div>)}</div>;
}
