'use client';
import {useState} from 'react';
export function FutureNote({note}:{note:string}){const [attempt,setAttempt]=useState(''),[revealed,setRevealed]=useState(false);return <aside className="future-note"><p className="eyebrow">A note from your earlier session</p><h3>Before you look: what was the useful idea?</h3><label>Try recalling it first<textarea aria-label="Recall before your note" value={attempt} onChange={e=>setAttempt(e.target.value)} maxLength={1000}/></label><button className="button-secondary" disabled={!attempt.trim()} onClick={()=>setRevealed(true)}>Reveal my earlier note</button><button className="button-text" onClick={()=>setRevealed(true)}>I don’t recall yet</button>{revealed&&<><blockquote>{note}</blockquote><p>Compare with your recall. What would you add or change? This reflection does not count as a checked answer.</p></>}</aside>;}
