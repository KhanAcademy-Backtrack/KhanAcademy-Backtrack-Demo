# Project continuity

Research checkpoint (10 September 2026): the user authorized publishing the full competitive research and source evidence in `docs/research/2026-09-10/`. Start with that directory's README and `BRILLIANT_RESEARCH_AND_DUNLO_DIRECTION.md` for the newest strategy. The next product priorities are substantially better original instructional animations/graphics and stronger BACKTRACK diagnosis, explanation, return and independent application, while preserving the complete study companion and free learner access. Khan should be integrated wherever a reviewed resource is useful, without forcing unrelated resources or unnecessary practice. Do not treat older product plans or slides as the current strategy. This checkpoint is research, not completed implementation. The user explicitly excluded the implementation prompt from Git; it is delivered directly in chat for manual transfer. Full research may be public in the repository, but competitor comparisons must still stay out of the learner-facing website, deck and application.

Latest scope: finish and publish the website fixes, preserving the full feature set. Slide design and PDF/Drive refresh are paused for strategy research. The user wants a manual handoff directly in chat for a researcher/planner, which must end with a plain-language TLDR and a ready-to-paste implementation prompt for a different agent. Never send it to another conversation. Keep competitor research out of public product, application and presentation content. Strengthen Philippine fit without reducing features or adding account/backend setup.

Before continuing substantial work, read these current project records:

1. `docs/PRODUCT_IMPLEMENTATION_PLAN.md` — the complete expanded product, problem framing, implementation sequence, and 15-slide story.
2. `docs/IMPLEMENTATION_TASKS.md` — current progress and remaining tasks.
3. `.refs/EXPANSION_ACTIVE.md`, when present — private execution checkpoint, source-file locations, browser/runtime details, and known issues.

The complete planning pass is finished. The user has explicitly instructed us to resume implementation and keep going. Existing uncommitted implementation work is preserved. Do not mistake an older completed BACKTRACK handoff for the status of this expansion. Follow subsequent user steering and update the task checklist as work proceeds.

Latest scope clarification: the user explicitly does NOT want backend/account setup for this version. Deliver the complete-feeling local experience and functional sharing. Do not ask for Supabase or account provisioning again. Avoid repeated demo/prototype/technical disclaimers in normal user flows. Do not fabricate live friends, synchronized group activity, or verified results; use functional shared scopes and clearly scoped local group play where useful.

Khan clarification: this is a study companion FOR Khan Academy. Add supported lesson/activity selection and recognized-URL entry; specific mapped Khan resources must appear within sessions and packs. Preserve context on external practice return. Collect completed/still-difficult/access-problem self-reports separately from fresh checks and teacher-confirmed reports. Public challenge entry and existing Khan learner entry must converge on the same packs/reviewer. Do not invent CSV fields or arbitrary URL conversion.

The public working identity is being developed; BACKTRACK remains the recovery engine. Preserve the existing repository, live entry URLs, saved progress, mathematical correctness, and the same Canva design. The expanded product includes ordinary study, packs, Today planning, review/Rematch, recovery, engagement, sharing/circles, creator/import workflows, Khan integration, and acquisition assets. Implementation order is based on dependencies.

Keep user-wide instructions in force: work in this conversation only; do not create, fork, delegate, or send work to another chat. Never reset credits. Ask before additional paid services. Existing authorization covers main pushes and Vercel deployment of verified project work; it does not authorize campaign posting or competition submission.

For this project, the user explicitly selected predominantly white readable surfaces, personality through useful interaction and the companion, no more than two font families, and an unchanged official UP Manila logo on every slide. The same Canva deck must remain exactly 15 slides. Keep its private editor URL out of public files and exports.

Latest visual direction (10 September): user supplied the exact Khan palette — green #14BF96, white #FFFFFF, navy #0A2A66. Apply it to the website and the same Canva deck. Navy is the readable text color; use bright green for the companion, major actions with navy labels, progress and emphasis. Use related tints for surfaces. Do not restore the earlier purple/orange theme. Preserve official Khan and UP Manila assets unchanged.

Handoffs requested by the user belong directly in the chat. Keep durable working notes too, but do not substitute a Drive link for the requested handoff text. Never claim unfinished or untested expanded features are complete.

Companion expression: user likes the blinking/closing eyes. Keep that animation, use a clear friendly smile, and omit the straight decorative line below the mouth that made the expression look stern.

Latest learner UX requirements: keep “How does this feel?” and all four confidence choices visible, with no accordion. “I don’t know yet” must be available in the challenge entry as well as the recovery checks; these responses should guide support, not award learning evidence. Align pack-card sections and actions across desktop columns despite unequal copy. Use Dunlo consistently in learner-facing copy. Public address is now https://dunlo.vercel.app/; the same Vercel project was renamed to dunlo (same project ID). Update current slides, QR codes and deliverable links to this address.

Science checkpoint (11 September 2026): chemistry and physics are now ordinary parts of Dunlo, not a separate section. Four destinations — `moles`, `balancing`, `motion`, `forces` — run on the existing BACKTRACK route engine, with real prerequisite chains that descend into the existing mathematics (`unit_convert → multiply`, `substitute → multiply`, `formula_mass → atom_count`). Two additive extensions carry them: a `subject` dimension on `TOPICS` (typed as `Record<Topic, TopicMeta>`), and an optional per-problem answer contract (`fields`, `tolerance`, `decimals`, `speak`) that defaults to the previous behaviour. `isCorrect` gained exactly one branch; `answerInputIssue` is unchanged; no mathematics generator changed, so `problemVersion` stays at 2 and every saved route keeps its meaning. All reviewed constants, compounds and balanced skeletons live in `src/lib/science.ts` with the rounding policy stated beside them. Original interactive explanations are in `src/components/study/ScienceLab.tsx`; each one predicts, manipulates, states the invariant, then hands over a fresh unexposed problem, and every serial it reveals is reserved.

Open item for the next session: no Khan Academy chemistry or physics URL could be verified. Khan is client-rendered and this machine's browsers could not load its application bundle, so nothing was guessed. Those steps are listed explicitly in `khan-materials.ts`'s `UNMATCHED` set — that listing is load-bearing, because the fall-through otherwise serves algebra factoring resources to a chemistry learner — and the interface says plainly that no matched resource exists. `docs/THIRD_PARTY_MATERIALS.md` records the exact procedure to add them once someone can open and title-check the pages. The constants table also still wants a science reviewer's pass, and the curriculum framing must be confirmed with the team rather than asserted as a grade level.

Presentation work stays paused. The "five mathematics areas" framing still needs a later deck pass in `docs/KEIC_WINNING_STRATEGY.md`, `docs/PITCH_AND_QA_PREP.md`, `docs/JUDGE_FINAL_REVIEW.md` and `docs/PITCH_DECK_15_SLIDES.md`.

Attribution: the user has asked that Claude and any other agent never appear as a co-author on commits or pull requests. Do not add `Co-Authored-By` trailers or generated-with footers to anything pushed from this repository.


Visual-learning release (11 September 2026): see docs/RELEASE_2026_09_11.md and docs/research/2026-09-11/LEARNING_DESIGN_DECISIONS.md. New maths generators use problemVersion3; preserve v1/v2 and per-attempt versions/expressions. Original MathsLab workspaces, contextual bookmark and first-visit spotlight tour must retain accessible/static/quiet paths. Routing probes are assisted evidence. Same-day repetition must not repeatedly advance spaced review. Quiz coverage receipts retain gaps in shared scope without private notes. Preserve all nine destinations including science. User wants smooth, simple, cute motion and personality that does not compete with thinking. Private ongoing execution checkpoint is .refs/NEXT_RELEASE_ACTIVE.md; never stage it or the original implementation prompt. Current user instructions explicitly authorize presentation work after product work, superseding earlier pauses.

Latest scope: edit only site and documentation for now. Do not edit the Canva presentation or exports until requested again. Keep learning claims provisional until real learner and retention evidence exists.
